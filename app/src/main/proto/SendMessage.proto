syntax = "proto3";
package tutorial;


message Foo{
  //用户名
  string name = 1;
  //本机IP 来判断是否为本地发送的
  string ip = 2;
  MessageType type = 3;
  //发送内容
  bytes data = 4;
}


//发送文件类型
enum MessageType{
  TEXT = 0;
  IMAGE = 1;
}